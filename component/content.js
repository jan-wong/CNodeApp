const content = `<div class="markdown-text">
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>
<p>遇到任何问题，请直接回复到 <a href="https://github.com/cnpm/cnpm/pull/187">https://github.com/cnpm/cnpm/pull/187</a> 。</p>
<h2>回滚</h2>
<p>一键回滚到稳定版本。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm -g
</code></pre>
<p><a href="https://github.com/cnpm/cnpm">cnpm cli</a> 的 install 速度已经是目前最快的一种 npm installer 了，最近 npm registry 推出了一种全新的 registry api 格式: <a href="https://github.com/npm/registry/blob/master/docs/responses/package-metadata.md#abbreviated-metadata-format">Abbreviated metadata format</a>，它可以大大减少install 过程中的网络请求数据大小，于是我们也在第一时间实现到 npminstall 中了。</p>
<p>为了更加平滑地更新过渡，我们计划使用2周时间来做beta测试，然后没什么大问题之后，将会发布正式版本的 cnpm<a href="/user/5">@5</a> 。</p>
<h2>尝鲜者来</h2>
<p>直接通过 beta 版本可以参与测试。</p>
<pre class="prettyprint language-bash"><code>$ cnpm install cnpm@beta -g 
</code></pre><h2>问题反馈</h2>

</div>`
export default content;